
<!DOCTYPE html>
<html>
  <head>
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <h1>📋 Message Board</h1>
    <% for (let i = 0; i < messages.length; i++) { %>
      <div class="message-card">
        <p><strong>User:</strong> <%= messages[i].user %></p>
        <p><strong>Message:</strong> <%= messages[i].text %></p>
        <p><small><strong>Date:</strong> <%= messages[i].added.toLocaleString() %></small></p>
        <a href="/message/<%= i %>">🔍 Open Message</a>
      </div>
    <% } %>
    <a href="/new"><button>➕ Add a New Message</button></a>
  </body>
</html>

